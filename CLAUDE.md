# ExportOS Web — 개발 인수인계 (Claude Code용)

## 프로젝트 개요
ExportOS는 수출 중소기업(전남 김·수산, 무역 전담 인력 0~1명인 신규·겸직 기업)을 위한
**수출 계약·결제 리스크 통합 관리 SaaS**의 MVP다. flex.team의 UX를 벤치마킹했다.

핵심 컨셉:
- 무역 거래 생애주기(리드·상담 → 계약 → 결제 → 선적·통관 → 회수 → 완료)를 **하나의 거래원장(Deal Ledger)** 위에서 관리
- 거래 등록 시 **결제·계약 위험을 규칙 엔진으로 자동 진단**(0~100점)하고 **선제 조치를 권유**
- 사용자가 매 단계 **검토·승인(칸반 드래그)** 해야 진행되는 승인 루프
- **회수 지연 예방이 목표이자 한계** — 대금 회수를 대신 집행하지 않는다(추심·소송 X). 확정·집행은 변호사·관세사·은행·K-SURE로 연결만 한다.

## 역할 경계 (절대 규칙 — 기능 추가 시 위반 금지)
- 하는 것: 정보 제공 · 리스크 표시 · 권유 · 전문기관 연결
- 안 하는 것: 계약 확정(변호사법), 은행급 L/C 서류심사, 보험 인수, 채권 회수 집행, 개별 바이어 실시간 수입내역 추적
- 모든 고위험 산출물에는 "참고용, 확정은 전문기관" 고지 유지

## 현재 구현 (index.html — 단일 파일, 순수 HTML/CSS/JS, 의존성 0)
품목 범용화 완료(김 특화 X — 카테고리: 수산식품/농식품/화장품/기계·부품/일반, CATREC로 품목별 통관 규칙 확장).

### 절차(태스크) 엔진 — 핵심 설계
- `STAGE_TASKS`: 6단계 × 표준 실무 절차 템플릿. 단계 진입 시 자동으로 할 일 생성(genStageTasks, deal.genStages로 중복 방지)
- **트랙 7종**(영업/계약/결제/물류·통관/서류/위험조치/기타)으로 동시 진행 업무를 색상 구분 — 병렬 업무가 엉키지 않게 하는 축
- 마감일: 단계 진입일 기준 off(D+n) 또는 결제 만기일 기준 dueOff(회수 단계)
- 위험 진단 recs → '위험조치' 태스크로 동기화(syncRiskTasks, 완료 상태 보존)
- **자동 팔로업**: 만기 초과·미회수 감지 시 독촉 태스크 자동 생성(autoFollowup)
- **품목×수입국 지식베이스(KB_RULES)**: 규칙 객체 배열 — 매칭축 cats(카테고리)/hs(HS 접두, 예: 1212.21 김)/countries('EU'는 회원국 확장). 매칭 시 note(서류·통관 탭·규칙 찾기 표시) + task(해당 단계 진입 시 할 일 자동 생성, src:'kb'). 수정 시 syncKbTasks로 재동기화(완료 보존·stale 제거), 부팅 시 기존 데이터 마이그레이션. 규칙 추가 = 배열에 객체 1개 추가
- **단계 게이트**: 필수(req) 절차 미완료 상태로 단계 이동 시 확인 모달(누락 목록 표시, 강행 시 로그 기록)

### 리스크 엔진 (INSTRUCTION_리스크엔진.md 구현 — 절대 규칙 준수)
- **위험 지표 탭**: assess()가 parts(항목별 브레이크다운) 반환 → "왜 이 점수인가" 표. 명칭은 "거래 위험 참고 지표(Risk Indicator)"(신용평가 표현 금지), 3단계 중심·점수 보조. 미확인 항목 N개 안내
- **내부/외부 분리**: buyerHistory()로 재거래 바이어(원장 동일 바이어명)는 내부 이력 표시, 신규는 "판단 불가" 문구 + K-SURE 신용조사 신청 상태(d.creditReq) 추적. 신규 바이어 신용 추측 금지
- **무역사기 대조기**: FRAUD_CHECKS(KOTRA 유형 기반) 체크리스트, d.fraud[], "신호 N개 — 주의"(단정 금지)
- **회수 시뮬레이터 탭**: exposure() 순수 산수(노출액=금액−선수금, 부보율은 사용자 입력 시만 반영), 시나리오 비교표, 회수 문안 4단계(D-7/D0/D+3/D+14, 국문+영문, 복사만·자동 발송 없음, currentDun으로 현재 단계 표시). d.sim에 입력 저장
- **K-SURE 개방데이터(KSURE_DATA)**: localStorage 별도 키(exportos_ksure_v1), 출고 시 빈 값(dataVersion null → "미탑재" 표시). [데이터 관리] 뷰에서 CSV 업로드→컬럼 매핑→기준일 필수→전량 저장(부분 저장 금지). isSample 샘플은 ⚠배지 필수. 90일 경과 시 갱신 권장. 모든 표시에 출처+기준일 병기. 폼 국가 입력 시 자동 표시(renderKsForm)
- **회귀 테스트**: selfTest() 9케이스(T1~T9) 부팅 시 실행, [데이터 관리]>자체 검증에 표시. 면책 문구 DISCLAIMER 고정

### 뷰 6개 (데이터 관리 포함)
- 대시보드: flex식 "오늘 확인할 일"(기한 초과 할 일/7일 내/회수 지연/고위험 미조치), KPI 4종(클릭 이동), 회수 임박·지연, 단계 분포
- **내 할 일**: 전 거래의 태스크를 기한 초과/7일 내/예정으로 그룹핑한 실행 인박스(트랙·거래 필터, 사이드바 배지) — 일일 실행은 여기, 칸반은 현황 파악용으로 이원화
- 파이프라인: 6단계 칸반(카드=위험·D-day·현 단계 절차 진행률 바·다음 할 일), 드래그 시 게이트 작동
- 거래원장: 검색/위험/단계/품목 필터, 절차 진행률 컬럼, **CSV 템플릿·가져오기**, JSON 내보내기
- 자료·연결: 전문기관 링크, 결제방식 가이드, **품목×수입국 규칙 찾기(KB 미리보기)**, 품목별 통관 유의, 표준 절차 전체 안내

### 거래 상세 (슬라이드 시트, 탭 6개)
- 개요: 위험 진단, 단계 스테퍼(클릭 이동·게이트), 현 단계 진행률, 위험조치 체크, 리포트 인쇄
- 절차·할 일: 단계별 아코디언(완료/진행 중/예정) × 트랙 그룹 체크리스트 + 수동 할 일 추가
- 계약: 방어 상태, 리스크관리 조항 자동 생성·복사 / 결제·회수: 만기·결제 태스크·K-SURE / 서류·통관: 서류·물류 태스크·품목 규칙 / 활동: 메모·로그
- 저장: localStorage(exportos_web_v5), 샘플 6건 시드(절차 일부 완료 상태로 시드)

## 규칙 엔진 (assess 함수)
- 결제방식 기본점수: O/A 40, 사후T/T 35, D/A 25, D/P 15, L/C 5, 선수금 0
- 신용등급 가산: D+35, C+25, 미확인+20, B+8
- 금액: $100k+15, $50k+8 / 검사조항 없음 +10 / 인코텀즈 없음 +6
- 60+ 고위험, 30+ 주의, 미만 양호
- 권유 규칙: 무담보+저신용→선수금·수출보험·L/C / 고액무담보→분할선적 / 조항미비→방어조항 / 신규시향국→검역·라벨

## 다음 개발 우선순위 (여기서 이어서)
1. ~~품목 지식베이스~~ 완료(KB_RULES, 규칙 20종) → **규칙 내용 확충**: 국가·품목별 규칙 추가는 KB_RULES 배열 확장만 하면 됨. 규칙 수가 커지면 별도 JSON/파일 분리
2. **백엔드 도입**: 서버 저장·다중 사용자(현재 localStorage 한계) — 가벼운 스택 권장
3. **공공데이터 연동 검토**: 관세청 수출입 통계 API(data.go.kr, 공개), K-SURE 국가신용등급(공개표) — 개별 바이어 API는 없음(신청 기반)
4. 알림(이메일/카톡) — 기한 초과·회수 임박 태스크 푸시 (백엔드 후)
5. 상태 기반 오케스트레이터(LangGraph류)로 규칙→할일 생성 자동화 (장기)

## 디자인 시스템
- 색: --pri #2b4a9b(네이비블루), 위험 red #d9482f / amber #c9871a / green #2e8b63
- 라운드 카드 + 은은한 그림자, Pretendard/맑은 고딕
- flex.team 벤치마크: 선제적 할 일 제안, 승인 워크플로우, 상태 뱃지, 드로어 상세

## 실행
브라우저에서 index.html 열기(빌드·서버 불필요). 데이터 초기화는 사이드바 하단 참고(localStorage 키 삭제).
