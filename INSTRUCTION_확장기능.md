# 구현 지시서 — 범용화 + 확장 기능 3종 (바이어 조사·시작점 포괄·수출 절차)

> 대상: exportos-web (배포본 포함)
> 전제: INSTRUCTION_리스크엔진.md의 [절대 규칙]과 UI_설계서.md의 금지·주의를 그대로 적용한다.
> 우선순위: 0번(범용화)이 최우선. 이후 1→2→3 순서로 구현하고 단계별 보고할 것.

---

## 0. 범용화 — "김" 하드코딩 제거 (최우선)

### 0-1. 사용자·회사 프로필 동적화
- 아바타 "김", 인사말 "김 대표님" 등 특정 사용자 이름 하드코딩을 전부 제거한다.
- 최초 접속 시(또는 설정 > 회사 정보) 온보딩 입력: 이름, 회사명, 주력 품목(HS 선택/입력), 연락처.
- 아바타 = 입력된 이름의 첫 글자, 인사말 = "{이름}님". 미입력 시 중립 표시("사용자").
- 프로필은 localStorage(또는 배포본의 저장 계층)에 저장하고 전 화면에서 참조한다.

### 0-2. 품목 중립화
- placeholder·샘플·예시에서 김(1212.21) 고정 표기를 제거하고 품목 중립 예시로 교체
  (예: "예: 화장품(3304), 기계부품(8409), 마른김(1212) …").
- 회사 프로필의 주력 품목이 설정되면 신규 거래 폼의 기본값·예시가 그 품목을 따르게 한다.
- 시드(샘플) 거래는 품목이 서로 다른 3~4건으로 다양화한다.

---

## 1. 바이어 조사 범위 확장 — "통합 바이어 조사 패널"

> 전제(정확성): K-SURE의 개별 바이어 DB는 개방되어 있지 않다(신청 기반 신용조사).
> 따라서 "검색하면 바이어 정보가 나오는" 기능을 흉내 내지 말 것. 아래는 조사 경로의 통합이다.

바이어 상세(또는 신규 바이어 등록)에 "조사 패널" 섹션 추가:
1. **자동 표시(개방 데이터)**: 국가·업종 입력 시 K-SURE 국가등급·OECD 등급·RISK INDEX 표시(데이터 탑재 시, 기존 3절 로직 재사용. 미탑재면 "데이터 미탑재").
2. **K-SURE 국외기업 신용조사 신청·추적**: [신청 안내 ↗](K-SURE 링크) + 상태 필드(미신청/신청함/결과 수신) + 결과 수신 시 신용등급 수기 입력 → 위험 지표에 반영. 상태 변경은 활동 로그에 기록.
3. **공개 기업등록부 링크 모음**: 국가 선택 시 해당국 공개 레지스트리 바로가기 표시.
   - 데이터 구조: `REGISTRIES = [{country, name, url, note}]` — **실존 확인된 공식 레지스트리만** 수록(예: 영국 Companies House, 싱가포르 ACRA, 미국은 주별 Secretary of State 안내 페이지). 불확실한 국가는 목록에서 제외하고 "공식 등록부 정보 없음 — KOTRA 무역관 확인 권장" 표시. **임의 URL 생성 금지.**
4. **사기 패턴 체크(기존)를 이 패널에 통합** — 조사와 패턴 점검이 한 화면.
5. **조사 이력 아카이브**: 조사 결과·날짜를 바이어 엔티티에 저장, 재거래 시 표시.

---

## 2. 시작점·서류 포괄 — "어디서 시작하든 쓸 수 있게"

### 2-1. 시작점 선택(거래 등록 1단계)
- 신규 거래 첫 질문: "이 거래는 지금 어디까지 진행됐나요?"
  선택지: 상담만 진행 / 계약 협의 중 / 계약 완료 / 선적 완료 / 대금 회수 대기
- 선택한 단계로 stage를 초기화하고, **그 단계부터의 할 일·권장 조치만 생성**한다. 이전 단계 정보(계약 조건 등)는 "나중에 입력" 가능하게 하되, 미입력 항목은 위험 지표에서 '미확인' 처리(기존 규칙 유지).

### 2-2. 무역 서류 라이브러리
- 새 화면 [서류·일정 > 서류 라이브러리]: 서류 카드 카탈로그.
- 데이터 구조: `DOC_LIBRARY = [{id, name, nameEn, what(한줄), when(필요 시점/단계), who(발행 주체), needIf(조건: LC|FTA|보험|공통), link(공식 안내 링크, 확실한 것만)}]`
- 수록(설명은 일반론 수준으로 정확하게): 견적송장(PI), 상업송장(CI), 포장명세서(PL), 매매계약서, 선하증권(B/L)·항공화물운송장(AWB), 원산지증명서(C/O), 수출신고필증, 적하보험증권, 신용장(L/C) 및 선적서류 세트, 환어음. 세부 법적 요건 단정 금지 — "자세한 요건은 은행/관세사 확인".
- 검색 + Cmd+K 연동("서류 검색") + 카테고리 필터(공통/L\/C 거래/FTA/보험).
- **거래 연계**: 거래 조건에 따라 서류함 슬롯 자동 구성(needIf 매칭: L/C면 선적서류 세트 추가, FTA 활용이면 C/O 추가, 부보 시 보험증권 추가).

---

## 3. 수출 절차 내비게이터 — 관세청 연계(정보·체크리스트·링크)

> 전제(정확성): 수출신고 API 직접 연동은 신고인 자격이 필요하므로 하지 않는다.
> 이 기능은 절차 정보의 구조화 + 기한 D-day + 공식 링크 연결이다. 절차 판단·신고 대행 아님을 화면에 고지.

### 3-1. 절차 체크리스트 (거래 ⑥선적·통관 단계에 자동 생성)
- 항목(각각 완료 체크 + 공식 링크):
  1) 수출신고 방식 결정 — 직접(유니패스) vs 관세사 위임 [유니패스 링크]
  2) 품목 수출요건 확인 — "HS별 요건확인 대상 여부를 유니패스에서 확인" [링크] (판정은 관세사·세관)
  3) FTA 활용 여부 결정 — 원산지증명 준비 [FTA포털·FTA-PASS 링크]
  4) 수출신고 및 신고필증 확보
  5) **적재 기한 확인 — 수출신고 수리일로부터 30일 이내 적재(연장 가능)** → 거래에 D-day 자동 등록
  6) 선적·적하보험 확인
  7) 관세환급 대상 여부 확인(소요량·간이정액) [안내 링크] (신청은 기업·관세사)
- 각 항목에 "놓치면 생기는 부담"을 한 줄로(예: 적재 기한 경과 시 신고 취하·과태료 위험 — 일반 안내 수준, 구체 금액 단정 금지).

### 3-2. 기한 D-day 통합
- 적재 기한·결제 만기·(부보 시) 보험 관련 일정을 거래 일정으로 통합, 기존 D-day 배지·오늘 확인할 일 위젯에 노출.

### 3-3. 규제·공지 자료실
- [연결·지원 > 자료·가이드]에 "관세청·기관 공지" 섹션: 큐레이션 링크 목록(제목·출처·게시일·확인일). **자동 수집 아님(수동 갱신)** 을 명시. 항목 임의 생성 금지 — 구조와 빈 상태만 구현하고 "관리자가 공지를 추가하세요" 상태로 출고.

---

## 검증 (완료의 정의)
- [ ] "김"·특정 이름 하드코딩 0건(코드 검색으로 확인), 프로필 미설정 시에도 정상 동작
- [ ] 품목 중립: 김 고정 표기 제거, 프로필 품목 반영 확인
- [ ] 조사 패널: 레지스트리 목록에 실존 확인 안 된 URL 없음, K-SURE 신청 상태 추적·로그 동작
- [ ] 시작점 선택에 따라 stage·할 일이 올바르게 생성, 미입력=미확인 처리 유지
- [ ] 서류 라이브러리 → 거래 서류함 자동 슬롯 매칭 동작
- [ ] 절차 체크리스트의 모든 링크가 공식 사이트로 연결, 적재 기한 D-day 등록 동작
- [ ] INSTRUCTION_리스크엔진.md 4-2 금지 검사 전 항목 재통과
- 완료 후: 구현 내역·한계(특히 데이터 불가 영역 처리 방식)·자체 발견 리스크 보고
