# 구현 지시서 — 바이어 위험 참고 지표 + 회수 노출액 시뮬레이터 + K-SURE 개방데이터 연동

> 대상: exportos-web/index.html (단일 파일 웹앱, CLAUDE.md 참조)
> 이 기능은 사용자가 실제 수출 의사결정에 쓰는 기능이다. **정보의 오류가 단 하나도 있어서는 안 된다.**
> 아래 [절대 규칙]은 기능 요구사항보다 우선한다. 충돌 시 절대 규칙을 따르고 기능을 축소하라.

---

## 0. 절대 규칙 (위반 시 구현 중단하고 보고할 것)

1. **사실 데이터를 지어내지 마라.** 국가 신용등급, RISK INDEX 값, 보험 요율, 수수료, 법령 조문 등
   실존하는 외부 사실은 **절대 임의 값으로 하드코딩하지 않는다.**
   - 데이터 구조(스키마)는 만들되, 값은 비워두거나 `isSample: true` 플래그가 붙은 명시적 샘플로만 넣는다.
   - 샘플 데이터는 UI에서 반드시 "⚠ 샘플 데이터 — 공공데이터포털에서 실데이터 교체 필요" 배지와 함께 표시된다.
   - 실데이터는 개발자가 data.go.kr에서 내려받아 CSV/JSON 가져오기 기능으로 탑재한다(아래 3절).
2. **결정론적 계산만 사용하라.** 위험 지표·노출액은 if-then 규칙과 산수로만 계산한다.
   LLM 호출, 랜덤, 추정 확률("미회수 확률 N%") 생성 금지.
3. **명칭 규정:** "신용평가", "신용등급 부여", "Credit Rating" 표현 금지(신용평가업 규제 저촉 소지).
   반드시 **"거래 위험 참고 지표(Risk Indicator)"** 로 표기한다.
4. **모든 외부 정보에 출처·기준일 태그.** 화면에 표시되는 모든 외부 유래 정보에는
   `출처 + 기준일`(예: "K-SURE 국가신용등급, 2026-06 기준")을 병기한다. 기준일 없는 외부 정보는 표시 금지.
5. **결측은 '미확인'으로.** 값이 없으면 0이나 기본값으로 계산하지 말고 "미확인" 상태로 처리하고
   위험 가산 + "미확인 항목 N개 — 확인 시 정확도 상승" 안내를 표시한다.
6. **면책 문구 고정.** 지표·시뮬레이터·권유 결과 하단에 항상:
   "본 지표는 입력 정보와 공개 데이터에 기반한 참고용입니다. 신용조사·보험·법률 판단은 K-SURE·은행·변호사가 수행합니다."
7. **역할 경계:** 기능의 효과는 표시·권유·연결·문안 생성까지. 자동 발송·자동 실행 금지(기존 CLAUDE.md와 동일).

---

## 1. 기능 A — 바이어 위험 참고 지표 엔진 (Risk Indicator)

### 1-1. 입력 (3분 입력 원칙: 필수 6개 이하)
필수: 품목, 시향국(국가), 거래금액(USD), 결제방식, 신용확인 여부(K-SURE 조사 완료/미완료), 결제 만기일
선택: 인코텀즈, 검사조항 유무, 신규 시향국 여부, 업종(수산식품/화장품/기계 등 — K-SURE RISK INDEX 매칭용)

### 1-2. 내부/외부 판단 분리 로직 (핵심)
- **재거래 바이어**(원장에 과거 거래 존재): 내부 이력으로 판정 —
  과거 결제 지연 횟수·클레임 여부를 집계해 "내부 거래이력 점수" 표시. 근거: "당사 원장 N건 기준".
- **신규 바이어**(이력 없음): 화면에 명시 —
  "신규 거래처 — 내부 정보만으로는 상대방 실체·지급능력을 판단할 수 없습니다."
  → K-SURE 국외기업 신용조사 신청 안내 버튼 + 신청 상태 추적(미신청/신청함/결과 수신) 필드.
  **앱이 신규 바이어의 신용을 추측하는 표현 일체 금지.**

### 1-3. 지표 계산 (결정론, 기존 assess 확장)
- 구성요소별 가점(기존 로직 유지): 결제방식(O/A 40 / 사후T/T 35 / D/A 25 / D/P 15 / L/C 5 / 선수금 0),
  신용확인 상태(D 35 / C 25 / 미확인 20 / B 8 / A 0), 금액($100k+ 15 / $50k+ 8),
  검사조항 없음 +10, 인코텀즈 미선택 +6.
- **분해 공개(설명가능성):** 결과 화면에 "왜 이 점수인가" 항목별 브레이크다운 표를 반드시 표시.
- 커뮤니케이션은 3단계(고위험/주의/양호) 중심, 점수는 보조.

### 1-4. 무역사기 패턴 대조기 (체크리스트 방식)
- KOTRA가 공표한 사기 유형(서류위조·결제사기·선적불량 등)을 **질문 체크리스트**로 구현:
  예) "첫 거래인데 대량 주문을 제안했다", "가격 협상 없이 수락했다", "결제 계좌 변경을 요청받았다",
  "서류 발송을 급하게 재촉한다", "회사 정보가 모호하거나 무료 웹메일을 쓴다".
- 체크 수에 따라 "사기 의심 신호 N개 — 주의" 표시. **"이 바이어는 사기다" 단정 표현 금지.**
- 각 항목에 근거 태그: "KOTRA 무역사기 유형 기반".

---

## 2. 기능 B — 회수 노출액 시뮬레이터 (Exposure Simulator)

### 2-1. 계산 (순수 산수 — 오류 불가 영역)
- 위험 노출액 = 거래금액 − 선수금 수령액 (즉 무담보 잔액)
- 입력: 거래금액, 선수금 비율(%), 분할선적 여부(회차별 금액)
- 출력 예: "선수금 30% → 위험 노출액 $35,000"
- **보험 부보 시 노출**: 요율·보상비율은 우리가 모르므로 **숫자를 만들지 말고**,
  "부보 시 노출액은 보험 조건(부보율·자기부담)에 따라 축소됩니다 — K-SURE 상담으로 확인" + 링크로 처리.
  (단, 사용자가 부보율을 직접 입력하면 그 값으로 계산해 표시 가능: "입력하신 부보율 90% 가정 시 노출 $3,500")
- 시나리오 비교 표: 현재 조건 vs 선수금 상향 vs 분할선적 vs L/C 전환 — 각각의 노출액을 나란히.
- 모든 결과에 "추정(입력값 기준)" 라벨.

### 2-2. 회수 시퀀스 문안 생성 (수동 발송)
- 만기 기준 자동 단계: D-7 사전 리마인드 / D-day 확인 / D+3 정중 독촉 / D+14 강한 독촉+후속 절차 안내.
- 각 단계의 이메일 문안(국문+영문)을 생성해 복사 버튼 제공. **자동 발송 금지.**
- D+14 문안에는 "K-SURE 보상 청구(부보 시)·법률 검토(미부보 시)" 다음 단계 안내 포함.
- 문안 어조: 사무적·정중, 법적 위협 표현 금지.

---

## 3. 기능 C — K-SURE 개방데이터 연동 (스냅샷 방식)

### 3-1. 데이터 구조 (값은 비워서 출고)
```js
const KSURE_DATA = {
  meta: { source: "한국무역보험공사 개방데이터 (data.go.kr)", dataVersion: null, loadedAt: null },
  countryRatings: [],   // {country, grade, evaluatedAt}
  oecdGrades: [],       // {country, prevGrade, currGrade}
  riskIndex: []         // {country, industry, ri}  // RI1~RI5
};
```
- **출고 시 값은 빈 배열.** dataVersion이 null이면 관련 UI는 "공공데이터 미탑재 — 관리 메뉴에서 탑재하세요" 상태 표시.
- 데모 필요 시 `isSample:true` 명시 샘플 3~4건만 허용(⚠샘플 배지 필수).

### 3-2. 데이터 탑재 기능 (관리 메뉴)
- "데이터 관리" 화면 추가: data.go.kr에서 받은 CSV/JSON 업로드 → 파싱 → KSURE_DATA에 저장(localStorage) →
  dataVersion(사용자 입력: 자료 기준일)과 loadedAt 기록.
- 파싱 실패·형식 불일치 시 저장하지 않고 오류 표시(부분 저장 금지).
- data.go.kr의 정확한 데이터셋명·컬럼은 개발 시점에 확인해 파서를 맞출 것(추측 금지, 실패 시 컬럼 매핑 UI 제공).

### 3-3. 자동 표시 (바이어 정보 입력 시)
- 사용자가 국가(+업종) 입력 → KSURE_DATA에서 조회:
  - 국가신용등급 + 평가일, OECD 등급 변화(개선/악화 화살표), RISK INDEX(국가×업종, RI1~RI5)
  - 각 항목에 "K-SURE 개방데이터, {dataVersion} 기준" 태그.
- 데이터에 해당 국가/업종이 없으면: "해당 데이터 없음"(미확인 처리, 절대 근사값 대체 금지).
- dataVersion이 90일 이상 경과 시 "⚠ 데이터 갱신 권장" 배지.
- **개별 바이어 신용정보는 개방 데이터에 없음** → 1-2의 신청 라우팅으로 연결(혼동 금지).

---

## 4. 검증·QA (구현 완료의 정의)

### 4-1. 테스트 정답표 (회귀 테스트 — 코드에 포함하고 실행해 통과 확인)
| # | 입력 | 기대 결과 |
|---|---|---|
| T1 | O/A, 미확인, $80,000, 조항무, 인코텀즈무 | 40+20+8+10+6=84 → 고위험 |
| T2 | L/C, A등급, $60,000, 조항유, FOB | 5+0+8+0+0=13 → 양호 |
| T3 | 사후T/T, C등급, $35,000, 조항무, CIF | 35+25+0+10+0=70 → 고위험 |
| T4 | 선수금T/T, B등급, $22,000, 조항유, DAP | 0+8+0+0+0=8 → 양호 |
| T5 | 노출액: $50,000, 선수금 30% | $35,000 |
| T6 | 노출액: $50,000, 선수금 30%, 사용자 입력 부보율 90% | $3,500 (가정 라벨 표시) |
| T7 | 신규 바이어 등록 | "내부 정보로 판단 불가" 문구 + 신용조사 신청 버튼 노출 |
| T8 | KSURE_DATA 미탑재 상태에서 국가 입력 | "공공데이터 미탑재" 표시, 등급 표시 안 됨 |
| T9 | 만기 D+3 경과 거래 | 독촉 문안(정중) 생성 버튼 노출 |

### 4-2. 금지 검사 (구현 후 자체 점검하고 결과를 보고할 것)
- [ ] 코드 전체에 임의의 국가등급·RI값·보험요율 하드코딩 없음 (샘플은 isSample+배지)
- [ ] "신용평가/등급 부여" 문구 없음
- [ ] 확률(%) 표현으로 미회수 가능성 단정하는 부분 없음
- [ ] 모든 외부 정보 표시에 출처·기준일 있음
- [ ] 자동 발송·자동 실행 코드 없음
- [ ] 면책 문구가 지표·시뮬레이터·문안 화면에 모두 존재

### 4-3. 완료 보고 형식
구현 후: ①테스트 정답표 통과 여부(각 케이스) ②금지 검사 체크 결과 ③스스로 발견한 리스크·한계를 보고할 것.

---

## 5. UI 배치 (기존 앱에 통합)
- 거래 상세 시트에 탭 추가/보강: "위험 지표"(브레이크다운+사기 체크리스트), "회수 시뮬레이터"(노출액+시나리오+문안).
- 신규 거래 등록 폼: 국가 입력 시 K-SURE 정보 자동 표시 영역(3-3).
- 사이드바에 "데이터 관리" 추가(3-2).
- 디자인은 기존 디자인 시스템(CLAUDE.md) 준수.
