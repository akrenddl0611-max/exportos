# 구현 지시서 — flex 벤치마킹 UI 개편 (UI_설계서.md 기반)

> 대상: exportos-web/index.html
> 전제: UI_설계서.md를 먼저 읽을 것. INSTRUCTION_리스크엔진.md의 [절대 규칙]은 이 작업에도 그대로 적용된다.
> 기존 기능(위험 진단 엔진·칸반·거래원장·D-day·조항 생성·활동 로그)은 유지하며 IA와 화면을 개편한다.

## 작업 순서 (단계별로 커밋, 각 단계 후 동작 확인)

### 1단계 — 레이아웃·IA 개편
1. 사이드바를 UI_설계서 2절의 그룹형 메뉴로 재구성(업무/바이어/리스크/서류·일정/인사이트/연결·지원/관리).
   - 아직 미구현 메뉴는 라우트만 만들고 flex식 빈 상태(아이콘+설명+CTA)로 둔다.
2. 홈 피드를 3열로: 중앙(시작 가이드 0/4 + 오늘 확인할 일[기존] + 최근 활동), 우측(미니 만기 캘린더 + 알림 요약).
   - 시작 가이드 4단계: 회사정보 입력 → K-SURE 데이터 탑재 → 첫 거래 등록 → 위험 진단 실행. 완료 상태는 localStorage로 추적, 전부 완료 시 접힘.
3. Cmd+K 커맨드 팔레트: 신규 거래 / 바이어 검색 / 위험 진단 / 독촉 문안 / 거래 검색. 키보드(↑↓·Enter·Esc) 지원.

### 2단계 — 리스크 화면
4. "위험 진단" 마법사(설계서 3-2): 거래 선택 → 3카드 준비도(바이어 확인/결제·보험/계약 방어, 각 상태 배지+실행 버튼) → 그라데이션 CTA → 기존 진단 결과 표시.
5. "리스크 규칙" 화면(설계서 3-3): 기존 assess 규칙과 KB_RULES(있다면)를 규칙 카드/표로 시각화. 카테고리 탭, 사용/미사용 토글(미사용 규칙은 진단에서 제외), 출처·기준일 필수 표기.
6. "거래 정책" 화면(설계서 3-4): 기본 정책 3종(무담보 한도 / 신규 바이어 선수금 하한 / 만기 리마인드 시점)을 편집 가능한 카드로. 거래 등록·상세에서 위반 감지 시 amber 경고 배너.

### 3단계 — 바이어·서류
7. "바이어" 분할 뷰(설계서 3-5): 거래에서 바이어 엔티티 분리(이름·국가·업종·신용상태). 우측 상세에 거래 이력·내부 이력 점수(결정론: 지연 횟수 기반)·신용조사 신청 상태(미신청/신청함/결과 수신).
8. "거래 서류함": 거래별 서류 슬롯(PI/계약서/B\/L/C\/O/보험증권), 상태 미등록→등록→확정(파일 업로드는 이름·메모 기록 수준, 실제 파일 저장은 범위 외 — localStorage 한계 명시).
9. "회수 캘린더": 월간 그리드에 만기 표시(지연=red, 임박=amber), 클릭 시 거래 상세.

### 4단계 — 인사이트·연결
10. "인사이트" 퍼널(설계서 3-6): 단계별 거래 수 퍼널 + 지표 카드(고위험 비율·부보 권유 이행률·회수 지연 발생률·평균 회수 소요일). 전부 원장 데이터의 결정론 집계. 데이터 없으면 빈 상태.
11. "수출보험(K-SURE)" 페이지: 소개 + "이런 거래에 권장"(무담보·고액·신규 바이어) + K-SURE 링크. 요율·보험료 숫자 표기 금지("상담으로 확인").
12. "무역 파트너스": 관세사/법률/무역실무 전문가 3카드(소개·역할·[상담 요청]—mailto 또는 안내 모달). 실존 업체명 임의 기재 금지(자리표시자 "제휴 예정").
13. "설정" 드로어: 회사 정보(수출자 프로필: 상호·품목·연락처 — 거래 등록 시 기본값으로 사용), 데이터 관리 링크, 감사 로그(전 거래 활동 로그 통합 뷰).

## 검증 (완료의 정의)
- [ ] 기존 기능 회귀 없음: 거래 등록→진단→칸반 이동→체크리스트→D-day→조항 복사 모두 동작
- [ ] INSTRUCTION_리스크엔진.md 4-2 금지 검사 전 항목 재통과
- [ ] 신규 화면 전부에서: 외부 데이터 출처·기준일 표기 / 임의 수치·업체명 하드코딩 없음 / 빈 상태 처리
- [ ] Cmd+K, 시작 가이드 진행 상태, 정책 위반 경고 동작 확인
- [ ] 단일 파일 유지가 어려우면 css/js 분리는 허용(같은 폴더 내), 외부 CDN 의존은 금지
- 완료 후: 단계별 구현 내역·미구현 잔여·자체 발견 리스크를 보고할 것
